class RegisterRequest{
    unicorn
    encodedImage

    constructor(unicorn, encodedImage) {
        this.unicorn = unicorn;
        this.encodedImage = encodedImage;
    }
}

export {RegisterRequest}